{
    "url": "/graphql",
    "method": "POST",
    "body": {
        "query": "{{listUsersFields(parameters)}}"
    },
    "response": {
        "iterate": "{{body.data.users.nodes}}",
        "output": {
            "label": "{{item.email}}",
            "value": "{{item}}"
        }
    },
    "pagination": {
        "mergeWithParent": true,
        "body": {
            "query": "{{listUsersPaginationFields(parameters)}}",
            "variables": {
                "page": "{{body.data.users.pagination.page + 1}}"
            }
        },
        "condition": "{{body.data.users.pagination.page < body.data.users.pagination.pages}}"
    }
}