{
    "url": "/graphql",
    "method": "POST",
    "headers": {
        "Content-Type": "application/json",
        "Authorization": "Bearer {{connection.apiKey}}"
    },
    "body": {
        "query": "mutation { trainingCampaignAddUser(trainingCampaignId: {{parameters.trainingCampaignId}}, userId: {{parameters.userId}}) { errors { field placeholders reason recordId } node } }"
    },
    "response": {
        "output": "{{body}}"
    }
}